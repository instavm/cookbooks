# SEO Blog Pipeline (Recipe #16)

`POST /topic` generates an SEO blog draft; `GET /preview` renders the latest HTML.

## Verify

1. `POST /topic?dry_run=1` with `{"topic":"your keyword"}`
2. `GET /preview`
