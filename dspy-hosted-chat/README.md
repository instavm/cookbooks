# DSPy Hosted Chat

DSPy starter app configured for an OpenAI-compatible hosted model endpoint rather than a local GPU-backed runtime.

The default manifest and app code target a small hosted Gemma-family model through OpenRouter, while still letting advanced users override the provider URL or model name.
